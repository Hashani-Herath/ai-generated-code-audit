name: CodeShield Security Scan

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  security_scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # Replaces pip install and the manual CLI command
      - name: Run CodeShield Security Scan
        uses: Lydiamorgan85/codeshield-ai@main
